Replace prompt placeholders. Run this command prior to /run-prompts.

When prompted, input the 2 required values:
1. Deliverable Title / Topic
2. Goal & Acceptance Criteria

!python3 "$CLAUDE_PROJECT_DIR/prompt-runner/run.py"
